{
  "name": "starter-exercise",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile:sass": "node-sass sass/main.scss css/style.css",
    "watch:sass": "yarn run compile:sass && yarn run compile:sass -w"
  },
  "devDependencies": {
    "node-sass": "^4.12.0"
  }
}
